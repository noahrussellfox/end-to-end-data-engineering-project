sources:
  - name: raw_data
    database: fox-dev-449222
    schema: raw_data
    freshness:
      warn_after:
        count: 24
        period: hour
    loaded_at_field: "_airbyte_extracted_at"
    tables:
      - name: faker_products
      - name: faker_purchases
      - name: faker_users

